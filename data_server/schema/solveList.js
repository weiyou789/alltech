/**
 * Created by root on 2017/06/22.
 */
/*
import mongoose from 'mongoose'
import Report from './web/web_report'
const Schema = mongoose.Schema;
let WebresSchema = new Schema({
    ...Report,
    dom: { type: Number },
    load: { type: Number },
    res: [{ type: mongoose.Schema.Types.ObjectId, ref: 'ResTime' }],
  }, { timestamps: true });

export default (db = mongoose) => db.model('Webres', WebresSchema);*/
